#include <stdio.h>
#include <stdlib.h>

int main()
{
    int count = 5;
    count++; //Increment
    int total = 10;
    total += 3; //Add 3 to total

    printf("updated count: %d\n",count);
    printf("updated Total: %d\n",total);

    return 0;
}
