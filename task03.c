#include <stdio.h>
  int main(){
   /*
     int AccountBalance = 5000;
     int account_balance =4500;
     int 2ndtransaction = 200; // 2ndtransaction we cant write fist number 2 so can write secandetransaction
     int transaction Amount = 150; // transaction Amount
     int float =100; // we cant write "float" variable  name to data type */

     int AccountBalance = 5000;
     int account_balance =4500;
     int secandetransaction = 200; // 2ndtransaction we cant write fist number 2 so can write secandetransaction
     int transaction_Amount = 150; // transaction Amount
     int thirdtransaction =100; // we cant write "float" variable  name to data type

     printf("AccountBalance is :%d\n",AccountBalance);
     printf("account_balance is :%d\n",account_balance);
     printf("secandetransaction is :%d\n",secandetransaction);
     printf("transaction_Amoun is :%d\n",transaction_Amount);
     printf("AccountBalance is :%d\n",thirdtransaction);

     return 0;

  }
