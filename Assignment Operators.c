#include <stdio.h>

int main()
{
    int count=5;
    count++;  //increment
    int total=10;
    total+=3;//add 3 to the total

    printf("updated count : %d\n",count);
    printf("updated count : %d\n",total);
    return 0;
}
