#include <stdio.h>
#include <stdlib.h>

int main()
{
    int temperatur =30;
    if (temperatur>25) {
        printf("It's warm day,because temperature up to 25.");
    }
    else {
        printf("Today is a normal day");
    }
    return 0;
}
