#include <stdio.h>
int main () {
    int a = 5;
    int b = 10;
    int c = 15;

    int largest= (a>b) ? (a>c)? (b>a) ? (b>c) ? (c>a) ? (c>b);
    printf("Large number:%d\n,largest");
    return 0;







}
