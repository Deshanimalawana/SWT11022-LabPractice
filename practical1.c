#include <stdio.h>
int main(){
    int x = 10, y=4;
    int sum =x+y ;
    int defference = x-y;
    int product = x * y;
    int quotient = x / y;
    int remander = x %y;

    printf ("sum; %d\n",sum);
    printf ("defferencep: %d\n",defference);
    printf ("product: %d\n",product);
    printf ("quotien: %d\n",quotient);
    printf ("remander: %d\n",remander);

    return 0;

}
