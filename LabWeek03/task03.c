#include <stdio.h>

int main(){
int AccountBalance = 5000;
int account_balance = 4500;
//int 2ndTransaction = 200; invalid variable or constant name can't start with number.
//int transaction Amount = 150; variable name can't have space
//int float = 100; invalid float is a reserved keyword.

int ndTransaction = 200;
int num = 100;
int transactionAmount = 150;

printf("%d,%d,%d",ndTransaction,num, transactionAmount);
}
