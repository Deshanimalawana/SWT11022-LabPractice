#include <stdio.h>

int main() {
    int x = 10, y = 15;

    // Perform bitwise operations
    printf("Bitwise AND (x & y): %d\n", x & y);
    printf("Bitwise OR (x | y): %d\n", x | y);
    printf("Bitwise XOR (x ^ y): %d\n", x ^ y);

    return 0;
}
