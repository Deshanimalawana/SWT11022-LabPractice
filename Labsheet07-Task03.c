#include <stdio.h>

int main() {
    int num = 1;
    while (num <= 10) {
        if (num ) {
            printf("%d ",num);
        }
        num++;
    }
    printf("\n");

    return 0;
}

