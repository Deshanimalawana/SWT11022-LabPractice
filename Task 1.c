#include <stdio.h>
int prime(int num) {
      if (number>0){
      if(num % num == 0 ){
            return 1;}
      else{
            return 0;}
}

int main(){
    int number;
    printf("enter the number :");
    scanf("%d",&number);
    prime(number);

          printf("this is prime number");}

return 0;
}
