#include <stdio.h>
int main()
{
    int reg = 55;
    float temp = 65.25;
    char grade = 'A';

    printf("Registration No : %d\nTemperature : %.2f\nGrade : %c", reg, temp, grade);

    return 0;
}
