#include <stdio.h>

int main() {
    // Define length and width
    double length = 25.12428, width = 12.59;

    // Calculate area
    double area = length * width;

    // Display result with four decimal places
    printf("Area of the rectangle: %.4f cm²\n", area);

    return 0;
}
